#!/usr/bin/python

#divide without using division, mod or multiplication

def divide(dividend, divisor):
        finished=True
        result=0
        while finished:
        	divident -=divisor
        	result +=1
       		if divident<divisor:
		        finished=False
        return result
d1=12
d2=3
print divide(d1,d2)
